name: CI/CD Test

on:
  push:
    branches:
      - main

jobs:
  run-laptop:
    runs-on: self-hosted

    steps:
      - name: Get code
        uses: actions/checkout@v3

      - name: Show files
        run: ls

      - name: Run message
        run: echo "CI/CD working on laptop 🚀"